        {{> layout/desktop/css}}
        {{{body}}}

        {{> layout/desktop/javascripts}}

        {{> javascripts/javascripts}}


  {{#if templates}}
    <script id="templates_{{_app}}">
        (function () {
            var template  = Handlebars.template,
                templates = Handlebars.templates = Handlebars.templates || {};

          {{#templates}}
            templates['{{{name}}}'] = template({{{template}}});
          {{/templates}}
        }());
    </script>
  {{/if}}
